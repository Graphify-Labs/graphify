--- graphify/__main__.py
+++ graphify/__main__.py
@@ -3709,7 +3709,7 @@
             print(
                 "Usage: graphify extract <path> [--backend gemini|kimi|claude|openai|deepseek|ollama] "
                 "[--model M] [--mode deep] [--out DIR] [--google-workspace] [--no-cluster] "
-                "[--max-workers N] [--token-budget N] [--max-concurrency N] "
+                "[--skip-semantic] [--max-workers N] [--token-budget N] [--max-concurrency N] "
                 "[--api-timeout S]",
                 file=sys.stderr,
             )
@@ -3725,6 +3725,7 @@
         out_dir: Path | None = None
         no_cluster = False
         dedup_llm = False
+        skip_semantic = False
         google_workspace = False
         global_merge = False
         global_repo_tag: str | None = None
@@ -3771,6 +3772,8 @@
                 no_cluster = True; i += 1
             elif a == "--dedup-llm":
                 dedup_llm = True; i += 1
+            elif a == "--skip-semantic":
+                skip_semantic = True; i += 1
             elif a == "--google-workspace":
                 google_workspace = True; i += 1
             elif a == "--global":
@@ -3893,6 +3896,10 @@
             transcript_paths = _transcribe_all([str(p) for p in video_files])
             doc_files.extend(Path(p) for p in transcript_paths)

+        if skip_semantic:
+            doc_files = []
+            paper_files = []
+            image_files = []
+            dedup_llm = False
+            print("[graphify extract] skipping semantic extraction (--skip-semantic)")
+
         semantic_files = doc_files + paper_files + image_files
         if incremental_mode:
             print(
