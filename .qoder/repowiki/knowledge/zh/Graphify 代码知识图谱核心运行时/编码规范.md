- 新增语言 extractor 需在 `extractors/__init__.py` 的 `LANGUAGE_EXTRACTORS` 字典中注册，并在 `extract.py` 顶部补齐 re-export 以保持向后兼容。
- 跨子模块共享的中间数据结构（节点/边 dict、hyperedge 字段）在 `build.py` 中集中做容错与归一化，下游不再重复校验。
- 对外暴露的公共 API 一律通过 `graphify/__init__.py` 的 `__getattr__` 懒加载路由，避免顶层导入时加载可选依赖。