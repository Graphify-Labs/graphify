OpenAI SDK（兼容 Kimi/Gemini/Ollama）、Anthropic SDK、boto3 Bedrock Converse、Azure OpenAI SDK、Claude CLI (`claude -p`)；Git hooks 通过 `git rev-parse --git-path hooks` 解析 core.hooksPath，并通过 `subprocess` 调用 `gh` CLI 获取 PR 数据。