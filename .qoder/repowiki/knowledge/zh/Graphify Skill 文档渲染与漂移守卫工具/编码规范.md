- 所有模板占位符采用 `@@NAME@@` 双 at 符号风格，渲染后若仍有残留则抛出带未填占位符列表的 ValueError。
- 平台差异通过 `platforms.toml` 声明式描述（bucket/core/dispatch/extraction/shell/hooks_variant 等），Python 侧只做解析与替换，禁止硬编码平台分支。
- 生成的 Markdown 一律经 `_normalise` 归一化 CRLF→LF 且末尾恰好一个换行，确保跨平台 byte-identical。
- 对 v8 基线的任何有意偏离都通过显式白名单（`_CONSOLIDATION_ALLOWLIST`、`ALWAYS_ON_SANCTIONED_EDITS`、`_SANCTIONED_MONOLITH_DIFFS`）登记，而非直接修改比较逻辑。
- CLI 子命令（--check/--bless/--audit-coverage/--schema-singleton/--monolith-roundtrip/--always-on-roundtrip）各自返回问题列表，由 `main` 汇总并以 exit code 表达成功/失败。